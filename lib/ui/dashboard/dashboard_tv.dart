import 'package:flutter/material.dart';

class DashboardTV extends StatelessWidget {
  const DashboardTV({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber);
  }
}
